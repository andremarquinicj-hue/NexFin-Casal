"use client"; import { useMemo,useState } from "react"; import { CreditCard, Plus, Trash2 } from "lucide-react"; import AppShell from "@/components/AppShell"; import Empty from "@/components/Empty"; import { useHouseholdData } from "@/components/useHouseholdData"; import { brl, monthKey } from "@/lib/finance"; import { createItem, removeItem } from "@/lib/firestore";
export default function Cartoes(){const {cards,transactions,householdId}=useHouseholdData();const [open,setOpen]=useState(false);const [form,setForm]=useState({name:"",bank:"",holder:"",limit:"",closingDay:"28",dueDay:"7"});const spent=useMemo(()=>{const m:Record<string,number>={};transactions.filter(t=>t.type==="card"&&t.dueDate.startsWith(monthKey())&&t.status!=="cancelled").forEach(t=>m[t.cardId||""]=(m[t.cardId||""]||0)+t.amountPlanned);return m},[transactions]);async function save(e:React.FormEvent){e.preventDefault();await createItem(householdId,"cards",{...form,limit:Number(form.limit.replace(",",".")),closingDay:Number(form.closingDay),dueDay:Number(form.dueDay)});setOpen(false)}return <AppShell title="Cartões" subtitle="Limites, faturas e compras parceladas"><div className="section-actions"><div><strong>Faturas previstas no mês</strong><span>{brl(transactions.filter(t=>t.type==="card"&&t.dueDate.startsWith(monthKey())).reduce((s,t)=>s+t.amountPlanned,0))}</span></div><button className="soft-btn" onClick={()=>setOpen(!open)}><Plus/>Adicionar cartão</button></div>{open&&<form className="inline-form panel" onSubmit={save}>{([['name','Nome do cartão'],['bank','Banco'],['holder','Titular'],['limit','Limite'],['closingDay','Dia fechamento'],['dueDay','Dia vencimento']] as const).map(([k,l])=><label key={k}>{l}<input required value={form[k]} onChange={e=>setForm({...form,[k]:e.target.value})}/></label>)}<button className="primary-btn">Salvar</button></form>}<div className="cards-grid">{cards.map(c=>{const use=spent[c.id]||0;return <article className="credit-card-ui" key={c.id}><button className="delete-float" onClick={()=>confirm("Excluir cartão?")&&removeItem(householdId,"cards",c.id)}><Trash2/></button><div className="cc-top"><CreditCard/><span>{c.bank}</span></div><h3>{c.name}</h3><p>{c.holder}</p><div className="limit-row"><div><small>Fatura prevista</small><strong>{brl(use)}</strong></div><div><small>Limite disponível</small><strong>{brl(Math.max(0,c.limit-use))}</strong></div></div><div className="progress dark"><i style={{width:`${Math.min(100,c.limit?use/c.limit*100:0)}%`}}/></div><small>Fecha dia {c.closingDay} · vence dia {c.dueDay}</small></article>})}{!cards.length&&<div className="panel"><Empty text="Nenhum cartão cadastrado."/></div>}</div></AppShell>}
